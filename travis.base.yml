services:
  - docker
language: minimal
before_install:
- docker pull gin078/aur-bldr
- env | grep "AUR" > .env
- docker run -it gin078/aur-bldr sudo pacman -Syyu --noconfirm --noprogress && sudo pacman -Syu --noconfirm --noprogress

script:
- docker run -it --env-file .env gin078/aur-bldr

env:
